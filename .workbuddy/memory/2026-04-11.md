# 2026-04-11 工作日志

## 服务启动
- 前端未启动，已重新启动 `npm run dev`（端口5173）
- 后端正常运行（端口3000）

## 数字→中文映射全面修复（22:30）
修复了6个前端文件 + 1个后端文件的数字显示问题：
- community/DemandSquare.vue：demand_type→中文、parseAudience()→中文、typeColors改数字键、筛选框用数字
- community/DemandDetail.vue：demandTypeName支持数字、target_audience→中文
- community/MerchantDetail.vue：resourceTypeMap改为标准12类型
- community/ResourceDetail.vue：memberLevelMap增加5:钻石会员
- admin/AuditDemands.vue：typeLabels/typeColors改数字0-5、筛选框用数字
- admin/Demands.vue：typeLabels/typeColors从中文键改数字键
- admin/Resources.vue：resource_type显示中文映射
- server/communityController.js：getDemands的type参数parseInt()

## 商家发布资源类型修复（22:42）
修复前端 PublishResource.vue 与管理后台9个资源类型不匹配的问题：

### 管理后台资源类型（9个）
1. 资金赞助 - 资金金额、适用场景
2. 物资支持 - 物资清单、有效期、领取方式
3. 人力服务 - 人数、服务时长、人员描述
4. 专业服务 - 服务类型、资质证明、服务区域、收费标准
5. 媒体宣传 - 媒体渠道、媒体描述
6. 就业岗位 - 岗位描述、招聘人数、岗位类型、薪资范围
7. 志愿服务 - 人数、服务时长、志愿者描述
8. 场地支持 - 场地描述、面积、容纳人数、设施、开放时间
9. 其他 - 通用字段

### 修改文件
- `client/src/views/merchant/PublishResource.vue`：移除硬编码12类型，添加9个类型对应字段
- `server/src/controllers/merchantController.js`：更新 RESOURCE_TYPE_MAP 和 INSERT/UPDATE 语句

## 问题诊断与修复（22:30）

### 问题描述
商家发布资源时，商家类型选择"专业服务"后，填写的字段（专业服务类型、服务范围、资质证明、收费标准等）在社区端详情页没有正确显示，反而显示的是资金赞助/物资捐赠/人力支持等不相关的字段。

### 问题根因
前端 PublishResource.vue 填写的很多字段**没有提交到后端**，导致数据库中这些字段为空。

### 修复内容

#### 1. 数据库字段扩展
**resources 表新增字段：**
- `professional_type` - 专业服务类型
- `tech_types` - 技术类型（JSON数组）
- `tech_service_type` - 技术服务方式
- `goods_expiry` - 物资有效期
- `fund_scenes` - 适用场景（JSON数组）
- `media_channels` - 媒体渠道（JSON数组）
- `valid_until` - 资源有效期

**demands 表新增字段：**
- `expert_count` - 专家人数
- `frequency` - 服务频次
- `need_visit` - 是否需要上门
- `fee` - 期望费用
- `expert_qualification` - 专家资质要求
- `space_name` - 空间名称
- `space_area` - 空间面积
- `space_types` - 空间类型
- `facilities` - 现有设施
- `open_hours` - 开放时间
- `cooperation_types` - 合作形式
- `brand_display_types` - 品牌展示方式
- `space_usage_desc` - 空间使用说明
- `volunteer_points` - 志愿服务积分
- `volunteer_max_points` - 积分上限
- `volunteer_count` - 志愿者人数
- `volunteer_desc` - 积分说明

#### 2. 后端修改
- `server/src/controllers/merchantController.js`
  - `createResource` 接口：添加所有新字段到INSERT语句
  - `updateResource` 接口：添加所有新字段到UPDATE语句
  - 修复INSERT语句VALUES占位符数量（从28个修正为29个）

#### 3. 前端修改
- `client/src/views/merchant/PublishResource.vue`
  - `submitResource` 函数：提交所有填写的字段
  - 提交字段包括：professional_type、pricingType、techTypes、techServiceType、fundScenes、mediaChannels、goodsExpiry、validUntil 等

- `client/src/views/community/ResourceDetail.vue`
  - 新增专业服务类型显示
  - 添加标签中文映射函数：
    - `getServiceScopeLabel` - 服务范围映射
    - `getPricingTypeLabel` - 收费标准映射
    - `getTechTypeLabel` - 技术类型映射
    - `getTechServiceTypeLabel` - 技术服务方式映射
    - `getMediaChannelLabel` - 媒体渠道映射
  - 新增计算属性：`techTypesList`、`mediaChannelsList`

#### 4. 测试验证
- 创建测试资源 ID=34（专业服务类型）
- 验证字段正确保存到数据库
- 页面预览：http://localhost:5173/community/resource/34

## 修改的文件清单
1. `server/src/controllers/merchantController.js` - 后端接口更新
2. `server/src/controllers/communityController.js` - 后端接口优化
3. `client/src/views/merchant/PublishResource.vue` - 前端提交逻辑
4. `client/src/views/community/ResourceDetail.vue` - 前端显示逻辑

## 数据库操作
使用以下SQL命令添加字段：
```sql
-- resources 表
ALTER TABLE resources ADD COLUMN professional_type VARCHAR(100) AFTER coverage;
ALTER TABLE resources ADD COLUMN tech_types JSON AFTER certification;
ALTER TABLE resources ADD COLUMN tech_service_type VARCHAR(50) AFTER tech_types;
ALTER TABLE resources ADD COLUMN goods_expiry DATE AFTER tech_service_type;
ALTER TABLE resources ADD COLUMN fund_scenes JSON AFTER price_range;
ALTER TABLE resources ADD COLUMN media_channels JSON AFTER media_type;
ALTER TABLE resources ADD COLUMN valid_until DATE AFTER expected_reward_desc;

-- demands 表
ALTER TABLE demands ADD COLUMN expert_count INT AFTER activity_type;
-- ... 其他字段类似
```

## 下午修复（2026-04-11 20:50）

### 问题1：商家端推荐需求卡片 demand_type 显示数字
- **原因**：Home.vue 直接显示 `demand.demand_type`（数字），没有中文映射
- **修复**：添加 demandTypeMap 映射（0=活动赞助, 1=专家服务, 2=空间运营, 3=物资赞助, 4=健康服务, 5=教育培训）和 getDemandTypeName() 函数
- **修改文件**：`client/src/views/merchant/Home.vue`

### 问题2：社区留言在管理后台看不到
- **原因**：后端 getComments 没有处理 status 参数
- **修复**：添加 status 参数处理，支持筛选全部/正常/已删除
- **修改文件**：`server/src/controllers/adminController.js`

### 部署状态
- 前端已构建并上传到服务器
- 后端已更新并重启 PM2
- API 测试正常（资源广场返回 200）
