omitted
- 20:18 服务重启（前端 Vite 崩溃）：端口 3000+5173 重启
- 20:23 前端再次崩溃重启
- 修复：vite.config.js 添加 `/uploads` 代理（解决图片 404）
- 社区资料保存 500：communityController updateProfile 两个 Bug：
  1. `data.latitude !== undefined` 对 null 不生效 → 改为 `!= null` + `!isNaN`
  2. MySQL ST_GeomFromText(?, 4326) 轴顺序问题 → 改用 SRID=0，POINT 经度在前纬度在后
- Vite 代理：后端 3000 / 前端 5173
- 21:42 手机端适配优化：社区/商家登录页与注册页
  - 验证码输入框+按钮改为垂直堆叠（flex-direction: column）
  - 表单元素手机端字号缩小
  - footer改为居中对齐
  - 容器padding和圆角优化
  - 极小屏幕（480px）减少margin/padding
