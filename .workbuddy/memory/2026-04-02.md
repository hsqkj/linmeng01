# 2026-04-02 工作日志

## Mock→API 迁移进度

### 本轮完成（22:30-23:00）

**admin.js API 完善：**
- 修复多处路径问题，对齐后端实际路由：
  - 社区管理：`/admin/users/communities`（含 users 前缀）
  - 商家管理：`/admin/users/merchants`
  - 大使管理：`/admin/users/ambassadors`
  - 会员配置：`/admin/config/members`（PUT）
  - 轮播图：`/admin/config/banners`
  - 标签：`/admin/config/tags`
  - 行政区划：`/admin/config/basic/regions`
  - 大使提成：`/admin/config/ambassador`（PUT）
  - 撮合奖励：`/admin/config/reward`（PUT）
  - 商家评级：`/admin/config/rating`（PUT）
- 修复 createAdmin/updateAdmin 字段映射（username/account → username）

**前端页面迁移：**
1. **UsersAmbassador.vue** — 完全重写，getAmbassadors 分页加载，getAmbassadorDetail 获取详情和提成记录，updateAmbassadorStatus 处理审核/禁用/恢复
2. **ConfigTags.vue** — 社区/商家/自定义标签三个 tab 全部对接 getTags/createTag/updateTag/deleteTag API
3. **ConfigBanner.vue** — 轮播图列表和 CRUD 全部对接 getBanners/createBanner/updateBanner/deleteBanner API
4. **ConfigMember.vue** — saveConfig 改为调用 saveMemberConfig API
5. **ConfigAmbassador.vue** — saveConfig 改为调用 saveAmbassadorConfig API，传入 level_commissions
6. **ConfigRating.vue** — saveAll 改为调用 saveRatingConfig API
7. **ConfigAdmin.vue** — 完全重写，getAdmins 加载列表，createAdmin/updateAdmin/deleteAdmin 实现 CRUD，status 使用数字 0/1
8. **ConfigBasic.vue** — 行政区划 tree 改为从 getRegions API 加载，支持 createRegion/updateRegion/deleteRegion API 操作

**遗留说明：**
- Messages.vue 系统通知 tab（静态 mock），后端无对应 API
- ConfigBasic.vue 其他 tab（活动类型/企业类型等）无后端 API，保持本地状态
- ConfigAlgorithm.vue（匹配算法）无后端 API，保持本地状态

## 第三轮完成（23:16）

**细节修复（管理后台）：**
- AuditDemands.vue：待审核数量从"12条"硬编码改为 `pendingCount` 动态显示
- AuditResources.vue：待审核数量从"8条"硬编码改为 `pendingCount` 动态显示
- UsersCommunity.vue：街道筛选选项从硬编码（上海街道）改为从 `getRegions` API 加载（level=2）
- UsersMerchant.vue：企业类型筛选从 7 类扩展为完整的 43 类常量（与注册页一致）

**Mock→API 迁移总结：**
- 管理后台 16 个页面全部已对接真实 API（部分无后端 API 的页面保留本地状态）
- 社区端/商家端/大使端 15+ 个页面已全部对接真实 API
- 所有修改文件通过 lint 检查

## 第四轮完成（23:22）- 新增系统通知 + 匹配算法API

**数据库：**
- 新增 `system_notifications` 表（IF NOT EXISTS）：id/title/content/target_type/target_ids/priority/status/published_at/created_by
- `sys_configs` 中 `match_algorithm` 默认数据格式升级为结构化 JSON（含 dimensions/maxResults/matchRadius）

**后端（server/src/）：**
- `controllers/adminController.js` 新增：
  - 系统通知 CRUD：getNotifications / getNotificationDetail / createNotification / updateNotification / deleteNotification / publishNotification
  - pushNotification() 辅助函数：发布时自动写入各端 comments 表
  - 匹配算法：getAlgorithmConfig / saveAlgorithmConfig
- `routes/admin.js` 新增：/admin/notifications* 路由组 + /admin/config/algorithm

**前端（client/src/）：**
- `api/admin.js` 新增：getNotifications / getNotificationDetail / createNotification / updateNotification / deleteNotification / publishNotification / getAlgorithmConfig / saveAlgorithmConfig
- `views/admin/Notifications.vue`（新建）：系统通知管理页面，支持创建/发布/撤回/删除
- `views/admin/ConfigAlgorithm.vue`（重构）：对接 getAlgorithmConfig / saveAlgorithmConfig，权重总和实时计算，maxResults/matchRadius 参数完整
- `layouts/AdminLayout.vue`：侧边栏新增"系统通知"菜单项
- `router/index.js`：新增 /admin/notifications 路由
