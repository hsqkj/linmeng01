# 2026-04-17 工作日志

## 短信验证码配置完成（10:55）

### 最终正确配置
- ACCOUNT_SID: `8aaf07085581a831015582428904010d`（容联云主账号）
- AUTH_TOKEN: `3478f34b326147de895c07879561e1f2`
- APP_ID: `2c94811c8cd4da0a018f24f97c0875d3`（"好社区订单通知"应用）
- TEMPLATE_ID: `3088517`（验证码模板，审核通过）

### 修复的问题
1. **IP 鉴权**：添加本地 IP `27.19.209.77` 到容联云白名单
2. **APP_ID 错误**：原来用 ACCOUNT_SID 代替 APP_ID，改为应用真实 APP ID
3. **Authorization 格式**：`Basic Base64(Sid:Token)` → `Base64(Sid:Timestamp)`（无 Basic 前缀）
4. **API 端口**：443 → 8883
5. **API 路径**：`/2015-04-12/.../TemplatesSMS` → `/2013-12-26/.../TemplateSMS`

### 更新的文件
- `server/.env.local`：RONGLIAN_APP_ID 更新为正确值
- `server/src/services/smsService.js`：Authorization 格式、端口、路径修复
- 服务器 `/var/www/linmeng/server/.env`：同步更新 APP_ID
- 服务器 pm2 以 `--update-env` 重启

### 测试结果
✅ `statusCode: 000000` 发送成功，手机 13986274016 收到验证码

## 服务器端API验证（11:10）
- 上传 `.env`（含容联云配置）和 `smsService.js` 到服务器
- pm2 `--update-env` 重启后端
- 服务器 curl 调用 `/api/public/sms/send` 返回 `{"code":200,"message":"验证码已发送"}`
- 日志确认容联云返回 `statusCode: 000000`，真实短信发送成功
- 短信验证码功能已完整上线
