# 2026-04-03

## Bug修复（晚间批次）

### 问题1：商家端需求详情页
- `server/src/routes/merchant.js`：将 `GET /demands/:id` 移到 `authMerchant` 中间件之后（路由顺序bug导致未登录时500）
- `server/src/controllers/merchantController.js`：`getDemandDetail` 添加 `req.merchant?.id` 空值保护 + 错误日志

### 问题2：商家端首页会员有效期
- `Home.vue`：`getProfile()` 改用 `Promise.allSettled` 并增加 `getMemberInfo()` API 调用获取 `expire_date`
- 模板已有 `{{ profile.member_expire_at || '—' }}`，后端正确返回则显示

### 问题3：会员等级数据不一致（首页普通 vs 会员页金牌）
- `Member.vue`：`currentLevel` 从硬编码 `{ level: 3 }` 改为从 `getMemberInfo()` API 动态加载
- 权益说明改为 `computed`，根据真实等级动态显示
- 添加 `loadMemberInfo()` + `onMounted` 自动加载

### 问题4：商家留言咨询回复后不显示
- `Messages.vue`：`loadComments()` 增加每条留言的回复加载（`getCommentReplies`）
- 留言卡片增加回复列表显示区块和样式
- 导入 `getCommentReplies` API

### 问题5：商家编辑资料不完整
- `Profile.vue`：编辑表单增加 Logo 图片URL字段 + 图文介绍图片URL字段
- `saveProfile()` 发送 `logo`、`images` 字段
- `startEdit()` 读取并初始化这些字段

### 问题6：社区端需求详情页空白
- 创建 `src/views/community/DemandDetail.vue`（完整的需求详情+意向管理+合作接受/婉拒）
- `router/index.js`：注册路由 `{ path: 'demands/:id', name: 'CommunityDemandDetail' }`
- `community/Demands.vue` 的"查看"按钮自动链接到该路由

### 问题7：商家资源详情页匹配度硬编码
- `merchant/ResourceDetail.vue`：`{{ '❤️'.repeat(4) }}` → `{{ '❤️'.repeat(currentResource.match_hearts || 0) }}`

### 问题8：编辑社区资料不完整
- `community/Profile.vue`：编辑表单增加 基本信息区块（社区名称/小区名称/行政区/街道/职务/手机/地址/Logo/场地图片）
- 字段为只读或可编辑（行政区划不可改，注册时已确定）
- 后端 `communityController.updateProfile`：增加 `position`、`address` 字段支持

### 问题9：管理后台手机端全屏
- `AdminLayout.vue`：顶部栏增加"全屏/退出全屏"切换按钮
- `mobileFullscreen` 状态变量，控制 `admin-layout` 的 `mobile-fullscreen` class
- CSS：当开启全屏时隐藏 `.mobile-header`，内容区占满屏幕

## 技术
- 前端构建：✅ 成功
- 后端API：localhost:3000 运行中
- 前端服务：localhost:5173 运行中

## 晚间批次修复（2026-04-03 20:20）

### 商家端
- `Home.vue`：首页推荐需求卡，社区名可点击弹出社区详情弹窗（含联系方式按会员等级显示）
- `Demands.vue`：需求广场增加区/街道/社区三级筛选；社区名可点击；getDemands 传 district/street/community 参数
- 后端 `merchantController.getDemands`：增加 street、community 参数支持
- 后端 `merchantController.updateProfile`：增加 social_identity、honors、expert_intro 三个字段支持
- 后端 `merchantController.getProfile`：返回这三个新字段
- `Profile.vue`（商家）：编辑表单新增"社会身份""荣誉资质""专家介绍"三个字段
- `DemandDetail.vue`：已有社区资料弹窗（按会员等级显示联系方式）

### 社区端
- 新建 `DemandSquare.vue`（需求广场）：展示所有社区需求，区筛选+排序（最新/热门）
- 路由新增 `/community/demands/square` → CommunityDemandSquare
- `CommunityLayout.vue`：顶部导航增加"需求广场"菜单项；手机端底部导航同步更新
- 后端 `communityController.getDemands`：增加 sort 参数（newest/hot），SELECT 增加 households 等画像字段
- `Resources.vue`（资源广场）：去掉"会员等级"筛选，改为"评价等级"筛选（对应平台星级），新增排序选项"评价最高"
- 后端 `community/getResources`：`level` 参数按 member_level 筛选（合理映射）
- `ResourceDetail.vue`：匹配度字段改为 matchHearts/matchScore（后端已返回）

### 管理后台
- `AdminLayout.vue`：手机端全屏模式下，通知图标点击跳转到 /admin/notifications

### 数据库
- merchants 表新增 social_identity、honors、expert_intro 三个 TEXT 字段

### 待办
- 需求广场：距离筛选需要坐标支持（暂无经纬度字段，暂用区级筛选代替）
- 资源广场评价等级筛选：后端按 member_level 近似实现（非 star_rating 精确筛选）
