# 邻盟 & 益邻邻 项目修改记录

**日期：** 2026-05-06

---

## 一、邻盟项目（LinMeng）

### 1. 社区端登录页优化

**文件：** `client/src/views/community/Login.vue`

| 修改项 | 说明 |
|--------|------|
| 移除微信登录标签页 | 改为独立的三种登录方式：验证码 / 密码 / 微信 |
| 微信登录区域精简 | 只保留微信授权登录按钮，移除手机号/密码输入框 |
| 验证码提示优化 | 去掉"模拟模式"和6位数字提示文字 |

**登录方式：**
- `https://www.3qall.com/#/login/community` - 社区端登录页
- `https://www.3qall.com/#/community` - 社区首页

---

### 2. 退出登录逻辑修复

**文件：** `client/src/layouts/CommunityLayout.vue`

| 修改项 | 说明 |
|--------|------|
| 退出登录跳转 | 改为 `location.href = '/#/community'` 直接跳转首页 |
| 原因 | 避免 Vue Router 守卫拦截跳转到多用户登录页 |

---

### 3. 微信公众号配置更新

**服务器：** `/var/www/linmeng/server/.env`

| 配置项 | 值 |
|--------|-----|
| AppID | `wxa382e1c9fb93780e` |
| AppSecret | `5584e3b1082f7e15388c05ed27122fd1` |

---

### 4. 后端数据库修复

**服务器：** `/var/www/linmeng/server/src/config/db.js`

| 修复项 | 说明 |
|--------|------|
| db.query 兼容方法 | 添加 `query()` 函数，兼容 `pool.query()` 语法 |

**数据库表：** `linmeng` 库

| 表名 | 修复内容 |
|------|----------|
| wechat_user_bind | 新建微信用户绑定表，用于存储 openid 与用户ID的关联 |

---

### 5. 微信登录流程完善

**文件：** `client/src/views/wechat/WechatLogin.vue`

| 修改项 | 说明 |
|--------|------|
| code 清除逻辑 | 获取 code 后立即从 URL 中清除，防止刷新重复使用 |

**登录流程：**
1. 首次登录：微信授权 → 绑定手机号 → 登录成功
2. 后续登录：微信授权 → 自动识别身份 → 直接登录

---

## 二、益邻邻项目（YiLinLin）

### 1. 益邻邻小程序配置

| 配置项 | 值 |
|--------|-----|
| AppID | `wx0d8ceb64dd56ca6c` |
| AppSecret | `833a30380f963fa5a537305c74938b9f` |

**服务器配置：** `/opt/yilinlin/backend/.env`

---

### 2. 后端数据库修复

**数据库：** `yilinlin_db`

#### users 表新增字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| gender | ENUM('unknown','male','female') | 性别 |
| bio | TEXT | 个人简介 |
| region_text | VARCHAR(255) | 地区文本 |
| organization_name | VARCHAR(100) | 组织名称 |
| contact_name | VARCHAR(50) | 联系人姓名 |
| position | VARCHAR(50) | 职位 |
| contact_phone | VARCHAR(20) | 联系电话 |
| description | TEXT | 描述 |
| publish_scope | VARCHAR(50) | 发布范围 |
| content_types | JSON | 内容类型 |
| certificates | JSON | 资质证书 |

#### publishers 表新增字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| cover_image | VARCHAR(255) | 封面图片 |
| position | VARCHAR(50) | 职位 |
| established_date | DATE | 成立日期 |
| member_count | INT | 成员数量 |

#### activities 表新增字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| check_in_staff | INT | 签到人员ID |
| publisher_name | VARCHAR(100) | 发布者名称（冗余字段） |
| follow_count | INT | 关注数量 |
| version | INT | 版本号 |
| news_content | TEXT | 新闻内容 |
| news_template_id | VARCHAR(50) | 新闻模板ID |
| news_generated_at | DATETIME | 新闻生成时间 |

#### 新建数据表

| 表名 | 说明 |
|------|------|
| publisher_followers | 发布者关注表 |
| activity_sessions | 活动场次表（含 venue_id, session_title, session_name, type 等字段） |
| venue_bookings | 场地预约表 |

---

### 3. 后端代码修复

**文件：** `/opt/yilinlin/backend/src/controllers/authController.js`

| 修复项 | 说明 |
|--------|------|
| gender 默认值 | `user.gender \|\| 0` → `user.gender \|\| 'unknown'` |
| 原因 | 数据库 gender 字段为 ENUM 类型，需要字符串默认值 |

---

### 4. 测试数据

**数据库：** `yilinlin_db`

| 类型 | 数据 |
|------|------|
| 发布者 | 测试社区居委会（id=1） |
| 活动 | 周末亲子手工活动、健康义诊活动、社区运动会（共3条） |

---

### 5. 业务域名验证

| 文件 | URL |
|------|-----|
| 验证文件 | `https://www.3qall.com/CW7sZ4t3Nd.txt` |

---

## 三、服务状态

### 后端服务

| 服务名 | 状态 | 端口 | 说明 |
|--------|------|------|------|
| linmeng-server | ✅ 在线 | 3000 | 邻盟后端 |
| yilinlin-api | ✅ 在线 | 3002 | 益邻邻后端 |

### 数据库

| 数据库 | 说明 |
|--------|------|
| linmeng | 邻盟数据库 |
| yilinlin_db | 益邻邻数据库 |

---

## 四、待处理事项

1. **益邻邻小程序代码修复**：`/saaa_config.json` 文件路径错误问题
2. **益邻邻管理后台**：完善活动详情页面的发布者信息展示
3. **益邻邻新闻生成功能**：后续接入腾讯云AI服务

---

## 五、访问地址

### 邻盟

| 地址 | 说明 |
|------|------|
| https://www.3qall.com/ | 邻盟官网 |
| https://www.3qall.com/#/login/community | 社区端登录 |
| https://www.3qall.com/#/community | 社区首页 |
| https://www.3qall.com/#/wechat-login | 微信授权页 |

### 益邻邻

| 地址 | 说明 |
|------|------|
| https://www.3qall.com/yilinlin/ | 益邻邻H5 |
| https://www.3qall.com/yilinlin-admin/ | 管理后台 |
| https://www.3qall.com/yilinlin-api/ | API接口 |

---

**生成时间：** 2026-05-06 20:05
